<Page
    x:Class="WebAppCSC4008.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebAppCSC4008"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Frame x:Name="ContentFrame">
            <StackPanel>
                <local:Header/>
                <TextBlock Text="Login" Margin="20" FontSize="30" />
                <TextBox x:Name="userTextBox" Header="Enter username:"/>
                <TextBlock x:Name="userTextBlock"/>
                <PasswordBox x:Name="passTextBox" Header="Enter Password:"/>
                <TextBlock x:Name="passTextBlock"/>
                <Button x:Name="Button1" Content="Login" 
                Click="Button_Click" />
                <TextBlock x:Name="loginTextBlock"/>
            </StackPanel>
        </Frame>
    </Grid>
</Page>
